# --- Development Environment Variables ---

# This is a fake tracking URI for local development.
# In a real scenario, this might point to a local MLflow container.
MLFLOW_TRACKING_URI="http://localhost:5000"

# A comma-separated list of valid API keys for the /admin endpoints.
API_KEYS="dev-key-1,dev-key-2"

# Service log level
LOG_LEVEL="debug"

# Allow extra fields in prediction requests for easier debugging
ALLOW_EXTRA="true"

# CORS Origins for local frontend development
CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"
